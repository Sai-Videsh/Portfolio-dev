╔════════════════════════════════════════════════════════════╗
║         PORTFOLIO BACKEND - QUICK REFERENCE                ║
╚════════════════════════════════════════════════════════════╝

📧 EMAIL DESTINATION: saividesh29@gmail.com

┌────────────────────────────────────────────────────────────┐
│ REQUIRED: Gmail App Password                              │
└────────────────────────────────────────────────────────────┘

1. Get App Password:
   → https://myaccount.google.com/apppasswords
   → Enable 2-Step Verification first
   → Create "Portfolio Backend" app password
   → Copy 16-character password (remove spaces)

2. Create server/.env file:
   
   PORT=5000
   EMAIL_USER=saividesh29@gmail.com
   EMAIL_APP_PASSWORD=[paste-16-char-password-here]

┌────────────────────────────────────────────────────────────┐
│ RUNNING THE APPLICATION                                   │
└────────────────────────────────────────────────────────────┘

✅ RECOMMENDED (Both together):
   npm run dev:all

📦 SEPARATE (Two terminals):
   Terminal 1: cd server && npm run dev
   Terminal 2: npm run dev

┌────────────────────────────────────────────────────────────┐
│ HOW IT WORKS                                              │
└────────────────────────────────────────────────────────────┘

User fills form → Frontend sends to backend → Email sent via
Gmail → You receive at saividesh29@gmail.com

Email includes:
• Subject: "Portfolio Review: [their subject]"
• Their name, email, message
• Terminal-themed HTML design
• Reply-To: [user's email]

┌────────────────────────────────────────────────────────────┐
│ URLS                                                       │
└────────────────────────────────────────────────────────────┘

Frontend: http://localhost:3000
Backend:  http://localhost:5000
Health:   http://localhost:5000/api/health

┌────────────────────────────────────────────────────────────┐
│ FILES CREATED                                             │
└────────────────────────────────────────────────────────────┘

server/
├── server.js          (Main backend code)
├── package.json       (Dependencies)
├── .env.example       (Template)
├── .env               (YOU CREATE THIS - contains App Password)
├── .gitignore         (Protects .env)
└── README.md          (Detailed docs)

Modified:
├── src/components/Contact/Contact.js (API integration)
├── src/components/Contact/Contact.css (Loading state)
├── package.json (Added dev:all script)
├── .gitignore (Protected server/.env)
└── BACKEND_SETUP.md (This guide)

┌────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                │
└────────────────────────────────────────────────────────────┘

1. Generate Gmail App Password (see link above)
2. Create server/.env file with your credentials
3. Run: npm run dev:all
4. Test contact form at http://localhost:3000
5. Check email at saividesh29@gmail.com

┌────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                           │
└────────────────────────────────────────────────────────────┘

❌ "Authentication failed"
   → Wrong App Password or 2-Step not enabled

❌ "Failed to send message"  
   → Backend not running or .env missing

❌ No email received
   → Check spam folder
   → Verify EMAIL_USER=saividesh29@gmail.com

❌ CORS error
   → Both servers must be running
   → Backend on port 5000, Frontend on 3000

┌────────────────────────────────────────────────────────────┐
│ SECURITY                                                  │
└────────────────────────────────────────────────────────────┘

✅ Safe to commit: All code files, README, .env.example
❌ NEVER commit: .env file (contains App Password)

The .gitignore is configured to protect your credentials.

╔════════════════════════════════════════════════════════════╗
║  Ready! Just add your App Password to server/.env         ║
╚════════════════════════════════════════════════════════════╝
